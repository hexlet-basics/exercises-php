---

theory: |
  Код на PHP представляет из себя набор инструкций, которые, как правило, отделяются друг от друга символом `;`.

```php
<?php

print_r('Mother of Dragons');
print_r('Dracarys!');
```

В примере выше последовательно печатаются в консоль две строчки. Каждая строчка — это инструкция, оканчивающаяся на `;`.

Теоретически инструкции могут идти и в одну строку:

```php
<?php

print_r('Mother of dragons'); print_r('Drakarias!');
```

но на практике такой подход считается плохим.

instructions: |
  Выведите на экран друг за другом три имени: "Robert", "Stannis", "Renly".

tips: []
