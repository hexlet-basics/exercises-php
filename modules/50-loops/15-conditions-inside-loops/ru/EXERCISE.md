Реализуйте функцию `countHashtags()`, которая принимает текст публикации и считает, сколько раз в нем встречается символ `#`.

```php
<?php

countHashtags('New post #php #code'); // 2
countHashtags('No tags here');        // 0
countHashtags('#start and #finish');  // 2
```
