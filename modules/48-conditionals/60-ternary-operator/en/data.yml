---
name: Ternary operator
definitions:
  - name: The ternary operator
    description: >
      is a way to turn a simple conditional instruction into an expression,
      e.g., `$number >= 0 ? $number : -$number`.
