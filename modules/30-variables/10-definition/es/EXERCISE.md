La URL del sitio se usa en varios lugares del programa. Para no duplicar la cadena, vamos a guardarla en una variable.

Crea una variable `$url` con el valor `https://hexlet.io` y muéstrala en pantalla dos veces — cada valor en su propia línea. Para el salto de línea usa `\n`.
