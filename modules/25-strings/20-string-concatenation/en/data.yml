---
name: Concatenation
tips:
  - >
    If the editor has `// BEGIN` and `// END`, then the code should be written
    between these lines.
definitions:
  - name: Concatenation
    description: >
      is the operation of joining two strings. For example, `print_r("King's " .
      ' Landing');`
