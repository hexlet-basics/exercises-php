---

name: Комментарии
theory: |
  Практически все языки программирования позволяют оставлять в коде комментарии. Они никак не используются интерпретатором и нужны исключительно для людей: чтобы программист оставлял пометки для себя и для других программистов. Комментарии в php бывают двух видов:

  **Однострочные комментарии** начинаются с `//`. Далее может следовать любой текст или набор символов, всё это не будет анализироваться и исполняться.

  Комментарий может занимать всю строку:

  ```php
  <?php

  // For Winterfell!
  ```

  или может находиться на строке после какого-нибудь кода:

  ```php
  <?php

  print_r('I am the King'); // For Lannisters!
  ```

  **Многострочные комментарии** начинаются с `/*` и заканчиваются на `*/`. Между ними каждая строчка должна начинаться с символа `*`.

  ```php
  <?php

  /*
   * The night is dark and
   * full of terrors.
   */
  print_r('I am the King');
  ```

instructions: |
  Создайте однострочный комментарий с текстом: `You know nothing, John Snow!`.

tips: []

definitions:
  - "Комментарий — текст в коде программы, который никак не влияет на функциональность и добавляется исключительно программистами для себя и своих коллег."