Вы получили код от коллеги — он работает правильно, но нарушает стандарт оформления PSR-12. Исправьте пробелы вокруг операторов, не меняя логику:

```php
<?php

print_r(    (5  **2)-(3*  7));
```

Результат должен остаться `4`.
