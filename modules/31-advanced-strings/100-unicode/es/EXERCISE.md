Muestra en pantalla la longitud de la cadena `Привет!` de dos maneras: primero con la función `strlen()`, y luego con `mb_strlen()`. Cada valor debe estar en su propia línea.

Así verás con tus propios ojos la diferencia entre la cantidad de bytes y la cantidad de caracteres en una cadena Unicode.
