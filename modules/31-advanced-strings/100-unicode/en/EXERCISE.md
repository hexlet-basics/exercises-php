Print the length of the string `Привет!` in two ways: first using the `strlen()` function, then using `mb_strlen()`. Each value should be on its own line.

This way, you'll see for yourself the difference between the number of bytes and the number of characters in a Unicode string.
