An online store sends the customer an order confirmation. The customer's name and the order number are already stored in variables — you need to substitute them into the message template using interpolation.

Use the variables `$username` and `$orderNumber` to print:

```text
Hello, Anna! Your order #1337 has been accepted.
```
